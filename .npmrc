# Ensure devDependencies are installed in Render build environment
production=false
# Speed up installs
prefer-offline=true
# Accept peer dependency resolution without failure (optional safety)
legacy-peer-deps=false
